version: '3.4'

services:
  eventmanagementapi:
    image: ${DOCKER_REGISTRY}eventmanagementapi
    build:
      context: .
      dockerfile: EventManagementAPI\Dockerfile

  tempwebapplication1:
    image: ${DOCKER_REGISTRY}orderprocessorapi
    build:
      context: .
      dockerfile: OrderProcessorAPI\Dockerfile

